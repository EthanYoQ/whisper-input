{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "selection-polish",
  "description": "Selection result events, shared appearance preferences and dragging only.",
  "windows": ["selection-polish"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:window:allow-start-dragging"
  ]
}
