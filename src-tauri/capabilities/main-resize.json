{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-resize",
  "description": "Custom resize handles belong only to the main window.",
  "windows": ["main"],
  "permissions": ["core:window:allow-start-resize-dragging"]
}
